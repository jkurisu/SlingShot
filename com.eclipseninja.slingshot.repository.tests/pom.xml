<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.eclipseninja</groupId>
	    <artifactId>com.eclipseninja.slingshot.releng</artifactId>
	    <version>0.1.0-SNAPSHOT</version>
		<relativePath>../com.eclipseninja.slingshot.releng/pom.xml</relativePath>
	</parent>

	<artifactId>com.eclipseninja.slingshot.repository.tests</artifactId>
	<packaging>eclipse-test-plugin</packaging>
	
	<name>Eclipse Ninja SlingShot Repository Tests</name>
	<description>Eclipse Ninja SlingShot Repository Tests Project.</description>
	
	<build>
		<plugins>
			<plugin>
				<groupId>org.eclipse.tycho</groupId>
				<artifactId>tycho-surefire-plugin</artifactId>
				<version>${tycho.version}</version>
				<configuration>
					<excludes>
						<!-- test mojo matches TestProject be default and treats it as PojoTest -->
						<exclude>**/Test*.class</exclude>
					</excludes>
					<useUIHarness>false</useUIHarness>
					<useUIThread>false</useUIThread>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>coverage</id>
			<properties>
				<emma.session.out.file>${project.build.directory}/emma/${project.artifactId}.es</emma.session.out.file>
				<emma.instrument.bundles>com.eclipseninja.slingshot.repository</emma.instrument.bundles>
			</properties>
			<build>
				<plugins>
					<plugin>
						<groupId>org.eclipse.tycho</groupId>
						<artifactId>tycho-surefire-plugin</artifactId>
						<version>${tycho.version}</version>
						<configuration>
							<excludes>
								<!-- test mojo matches TestProject be default and treats it as PojoTest -->
								<exclude>**/Test*.class</exclude>
							</excludes>
							<useUIHarness>false</useUIHarness>
							<argLine>-Xmx512M -XX:MaxPermSize=256M</argLine>
							<systemProperties combine.children="append">
								<emma.session.out.file>${emma.session.out.file}</emma.session.out.file>
								<eclemma.instrument.bundles>${emma.instrument.bundles}</eclemma.instrument.bundles>
							</systemProperties>
							<frameworkExtensions>
								<frameworkExtension>
									<groupId>org.eclemma.runtime.equinox</groupId>
									<artifactId>org.eclemma.runtime.equinox</artifactId>
									<version>1.1.0-SNAPSHOT</version>
								</frameworkExtension>
							</frameworkExtensions>
						</configuration>
						<dependencies>
							<dependency>
								<groupId>org.eclemma.runtime.equinox</groupId>
								<artifactId>org.eclemma.runtime.equinox</artifactId>
								<version>1.1.0-SNAPSHOT</version>
							</dependency>
						</dependencies>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-antrun-plugin</artifactId>
						<dependencies>
							<!-- <dependency>
								<groupId>emma</groupId>
								<artifactId>emma_ant</artifactId>
								<version>2.1.5320</version>
							</dependency>
							<dependency>
								<groupId>emma</groupId>
								<artifactId>emma</artifactId>
								<version>2.1.5320</version>
							</dependency> -->
							<dependency>
								<groupId>emma</groupId>
								<artifactId>emma_ant</artifactId>
								<version>2.0.5312</version>
							</dependency>
							<dependency>
								<groupId>emma</groupId>
								<artifactId>emma</artifactId>
								<version>2.0.5312</version>
							</dependency>
						</dependencies>
						<executions>
							<execution>
								<id>emma-report</id>
								<phase>verify</phase>
								<goals>
									<goal>run</goal>
								</goals>
								<configuration>
									<tasks>
										<taskdef resource="emma_ant.properties" />
										<echo>Process emma report...</echo>
										<emma enabled="true">
											<report>
												<infileset dir="${project.build.directory}/emma"
													includes="*.es" />
												<txt outfile="${project.build.directory}/emma/coverage.txt" />
												<xml outfile="${project.build.directory}/emma/coverage.xml" />
												<!-- Could also generate an HTML report, if you want... -->
											</report>
										</emma>
										<loadfile property="emma.txt"
											srcFile="${project.build.directory}/emma/coverage.txt"
											failonerror="false" />
										<echo>${emma.txt}</echo>
									</tasks>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>

		</profile>
	</profiles>

</project>